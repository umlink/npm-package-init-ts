import options from './interface/options';
const testMainFun = () => {
  const _options: options = {
    name: '小明',
    age: 18,
    height: '180cm',
    sex: '男',
  }
  return _options
}

export default testMainFun
